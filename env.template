# Скопируйте в .env.local (или .env) — файлы .env* игнорируются git.
BACKEND_URL=http://127.0.0.1:4000
# Опционально для pnpm db:seed — учётная запись SUPER_ADMIN (см. prisma/seed.ts).
# SEED_SUPERADMIN_LOGIN=
# SEED_SUPERADMIN_EMAIL=
# SEED_SUPERADMIN_PASSWORD=
# SEED_SUPERADMIN_NAME=
